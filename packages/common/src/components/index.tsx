import { ReactElement } from "react";
// import { primary } from "common/color";

export const Button = (): ReactElement => {
  return <button style={{ backgroundColor: "#0f62fe" }}>누르세요</button>;
};
